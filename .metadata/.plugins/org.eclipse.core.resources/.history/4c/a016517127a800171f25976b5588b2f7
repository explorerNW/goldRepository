package test6;

public class Test2 {
	(function(){
        return {
                   binaryFn:function(num){
                             return num.toString(2);
                   },
                   quickSort:function(array,low,high){
                              var count = 0; 
                                     if(high>=array.length || low != 0){
											return null;
									}
                                  Fn(array,low,high);                                                                     
                                         function Fn(array,low,high){
                                         if(low>high){
										       return null;
										 }
                                         var i = low;
                                         var j = high;
                                         var dk = array[low];
                                         while(i<j){
                                                while(i<j && array[j]<=dk){
												            j--;
												}
                                                while(i<j && array[i]>=dk){
												            i++;
												}
                                                 if(i != j){
     												  array[i] = [array[j],array[j] = array[i]][0];
//                                                         array[i] = array[i]+array[j];
//                                                         array[j] = array[i]-array[j];
//                                                         array[i] = array[i]-array[j];
                                                 }  										 
                                         }
											 array[low] = array[i];		
                                         array[i] = dk;	
										     Fn(array,low,i-1);
										 Fn(array,i+1,high);
                               }
									return array;                             
                   },
                   oneCount:function(){
							       for(i in str){
                                      if(str[i] === "1"){
                                             count++;
                                       }
                               }
                   }
                }
}())
}
